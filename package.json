{
  "name": "mqtt-simulator",
  "version": "1.0.0",
  "description": "This is a mqtt client and broker in the same app, the client publishes random data to the broker every second, and someone can subscribe to the broker to see these messages (I'm using this to test my TICK stack's Telegraf Configs.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon index.js"
  },
  "author": "Frederique Lemieux",
  "license": "ISC",
  "dependencies": {
    "mosca": "^2.8.3",
    "mqtt": "^2.18.8"
  }
}
